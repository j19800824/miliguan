# 项目协作规则

## 测试要求（强制）

凡是 AI 编写或修改的功能，**必须由 AI 自己测试验证通过后再交付**，不需要用户提醒。

- **后端接口**：用真实调用或复现调用验证（curl / 脚本 / 单测），确认返回符合预期。
- **外部服务**（OSS、PostgreSQL、Redis、短信、支付等）：做连通性或端到端实测，不能只改完配置就声称完成。
- **前端 / 页面**：确认实际可访问、关键交互可用。
- 能跑起来的就跑起来观察真实行为，**先验证、再下结论**；没验证不说"已完成/已修复"。
