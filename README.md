# miliguan

米粒冠项目 Monorepo 基础骨架，使用 `pnpm workspace + Turborepo` 管理多应用与共享包。

## 目录

- `apps/admin`：后台管理端
- `apps/store-app`：门店端安卓 App 预留应用位
- `packages/ui`：共享 UI 组件包

## 启动

```bash
pnpm install
pnpm dev
```

## 常用命令

```bash
pnpm build
pnpm typecheck
```
